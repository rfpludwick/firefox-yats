{
  "name": "yats",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "YATS (Yet Another Tab System) — Firefox extension fuzzy tab switcher",
  "scripts": {
    "build": "node scripts/build.mjs",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run lint && npm run test && npm run build"
  },
  "devDependencies": {
    "@eslint/js": "^9.28.0",
    "clean-css": "^5.3.3",
    "esbuild": "^0.25.5",
    "eslint": "^9.28.0",
    "globals": "^16.2.0",
    "html-minifier-terser": "^7.2.0",
    "vitest": "^3.2.4"
  }
}
